#include "buffer.h"

/* o buffer */

buffer_item buffer[BUFFER_SIZE];

int insert_item(buffer_item item)
{
    /* inserir item no buffer retornar 0 se for bem-sucedido, caso contrário return -1 indicando uma condição de erro */
}

int remove_item(buffer_item *item)
{
    /* remove um objeto do buffer colocando-o no item retornar 0 se for bem-sucedido, caso contrário return -1 indicando uma condição de erro */
}